// var artists = ['katy perry', 'taylor swift', 'spice girls'];

// var songs = ['this is how we do', 'shake it off', 'wannabe'];

// object {katy: 'this is how we do', taylor: 'shake it off', spicegirls: 'wannabe'}

// for (var i = 0; i < artists.length; i++) {
// 	console.log(songs[i] + artists[i]);


var car = {
	make: 'ford',
	color: 'blue',
	speed: 0,
	accelerate: function() { car.speed += 1; }
}