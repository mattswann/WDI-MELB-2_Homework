<%= form_for @dish do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :photo %>
	<%= f.file_field :photo %>

		<%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name %>

	<%= f.submit %>
<% end %>