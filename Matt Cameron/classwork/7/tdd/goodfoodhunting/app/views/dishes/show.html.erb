<div>
	<%= image_tag @dish.image_url %>
	<h2><%= @dish.title %></h2>
	<%= button_to 'edit', edit_dish_path, :method => :get %>
	<%= button_to 'delete', @dish, :method => :delete %>
</div>

<div>
	<% @dish.tags.each do |tag| %>
		<span><%= tag.name %> </span>
	<% end %>
</div>

